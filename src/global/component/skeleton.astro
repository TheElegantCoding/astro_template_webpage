---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'div'> &
{
  readonly class?: string;
  readonly isLoaded?: boolean;
};

const { class: classImage, isLoaded, ...rest } = Astro.props;
---
<div
  class='skeleton'
  class:list={[ !isLoaded && 'skeleton--active', classImage ]}
  {...rest}
>
  <slot />
</div>